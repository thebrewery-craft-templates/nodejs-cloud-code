apiVersion: v1
kind: Route
metadata:
  labels:
    app: {{ .Values.name }}
  name: {{ .Values.name }}
spec:
  host: {{ .Values.application_domain }}
  port:
    targetPort: {{ .Values.name }}
  tls:
    termination: edge
  to:
    kind: Service
    name: {{ .Values.name }}
